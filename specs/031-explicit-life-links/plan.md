# Task 41 Execution Plan

Status: COMPLETE. All stages passed; see `docs/audits/task-41-explicit-life-links.md`.

## Stage 0 — Activation and baseline

Confirm clean `main` and remote parity at `6bcffe751458ee37a4cde663e21336a1f484a613`, record the Task
39 feature checkpoint and Task 40 remediation baseline, measure the starting production bundle,
create Slice 031 and ADR 0035, activate Project State, synchronize governance surfaces, and run
activation governance.

## Stage 1 — Schema and Rust authority

Append migration 24 without editing migrations 1–23. Add the narrow `life_link` domain, DTO,
repository, and service boundary. Implement transactional endpoint validation, duplicate/self/cap
errors, create/remove, bulk live panel projection, and bounded existing-FTS target discovery.

## Stage 2 — IPC and Reader workflow

Register the four commands, generate TypeScript and permission TOML through accepted tooling, add
the explicit capability entries and centralized frontend adapters/query keys, then build a lazy
Reader panel and focused accessible dialog. Extend Reader history only as needed for exact-ID link
navigation and heading focus.

## Stage 3 — Lifecycle, recovery, and evidence

Map every tree/document/restore writer to precise link invalidation. Prove migration, domain,
projection, query shape, Vietnamese search, archive/restore, reopen, and full backup/restore.
Add native Windows workflow/restart/backup phases driven through accessible UI for link actions.

## Stage 4 — Performance, review, and closure

Measure the final bundle against the clean inventory. Preserve Task 40 evidence unless the approved
feature creates a tracked chunk or requires a truthful Task 41 aggregate transition within the
locked envelope. Run focused and broad gates, review the full baseline diff once, fix confirmed
findings, create the product checkpoint, close governance, commit, push, and confirm clean parity.

## Stop conditions

Stop before broadening scope if a permanent Life-node delete conflicts with FK protection; minimum
usefulness requires branch/anchor/package-remap authority; a dependency/workflow/broader schema or
Task 42 is required; or bundle growth exceeds 2 KiB startup, 24 KiB raw, or 8 KiB gzip.
