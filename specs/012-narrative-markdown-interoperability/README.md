# Spec 012 — Narrative Canvas Markdown Interoperability

Markdown export from a committed Narrative Canvas and Markdown import as a new Canvas on an empty leaf. Shared Basic Leaf Markdown authority. Three new IPC commands. No new migration.

See `spec.md`, `acceptance.md`, `tasks.md`.
