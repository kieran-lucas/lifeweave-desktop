Execute the complete Lifeweave Endgame Visual Redesign as the finite unattended program defined in `docs/visual/endgame-redesign/00_MASTER_EXECUTION_SPEC.md`.

This is explicit Product Owner authorization. The owner is intentionally unavailable. Read repository `AGENTS.md` and `AI_CONSTITUTION.md`, then the owner authorization, Master, current execution ledger, and only the files routed by the current packet. Do not preload `research-archive/`.

Execute exactly:
F0 → S01 → S02 → S03 → S04 → Q1 → S05 → S06 → Q2 → S07 → S08 → S09 → Q3 → S10 → S11 → Q4 → S12 → Q5 → FINAL.

At every stage, implement the fixed canonical rows, preserve real product capability/domain semantics, use the smart verification funnel, perform exactly one scoped review, freeze and resolve its in-scope finding set, persist ledger/evidence, create a scoped local commit/checkpoint, close that stage’s search space, then continue automatically. Do not wait for user confirmation for reversible in-scope frontend decisions; use the unattended decision policy and safest in-scope fallback.

Never invent capability, change backend/schema/domain/generated IPC/workflow seal, raise performance ceilings, push/force-push, overwrite unrelated work, or run recursive “keep improving until perfect” loops.

Old light goldens may change only through the staged reviewed baseline protocol. Later stages may reopen closed rows only for reproducible causal regressions.

The Goal is complete only when the Master terminal predicate is true: all 109 rows and Q1–Q5 are closed, mandatory final gates are satisfied, exactly one whole-app adversarial pass produced fixed `F_final`, `F_final` is resolved, final evidence/ledger is persisted, and the repository is auditable. Then STOP and report exact evidence.

If a true hard blocker has no safe in-scope route, record exact evidence and continue independent work where possible; never spin or ask the sleeping owner for a routine frontend choice.
