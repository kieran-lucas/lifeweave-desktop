# Task 48 Execution Plan

Status: ACTIVE.

## Stage 0 — Activation and baseline

Verify clean exact remote parity, read authority and current backup/Settings/native evidence, record
ADR 0042 and Slice 038, activate governance without product code, run source/governance/index gates,
commit and push, then measure the pre-product performance baseline.

## Stage 1 — Compatibility and inventory

Add Rust-owned compatibility and create-result DTOs. Keep strict restore parsing unchanged while a
separate bounded inventory path classifies version compatibility, validates supported package
shape, excludes artifacts, contains identity resolution, and sorts newest first.

## Stage 2 — Post-publication retention

After existing staged and final verification, protect the fresh backup and prune only excess older
currently restorable direct-child managed packages. Prove fixed 12, migration counting, incompatible
and artifact exemption, path containment, clock rollback, delete failure semantics, timer guard,
and unchanged format v2.

## Stage 3 — Settings extraction and confirmation

Generate bindings normally, move backup ownership from FoundationScreen to a lazy BackupSettings
surface, present version compatibility and retention policy, and add bounded accessible restore
confirmation with idle-only cancellation, focus containment/restoration, cache clearing, inventory
reload, and truthful create/cleanup announcements.

## Stage 4 — Native and load-bearing evidence

Register Phase 19 and exercise create, visible compatibility metadata, confirmation, cancellation
focus, real restore, and continued usability. Temporarily change the retention constant to 13,
record the meaningful focused-test failure, restore 12, and prove zero residue.

## Stage 5 — Performance, full gates, and product checkpoint

Measure final output, derive the versioned Task 48 budget mechanically under unchanged locked
ceilings, run focused/full Rust, frontend, native, build, RC, governance, and diff gates, review the
activation-to-product diff, then commit and push `implement managed backup retention`.

## Stage 6 — Closure

Write the Task 48 audit, close governance at schema 27/format2 with Task 49 prohibited, commit/push
closure, record its SHA in an audit-only commit, and verify a clean worktree at remote parity.
