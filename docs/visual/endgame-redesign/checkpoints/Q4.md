# Checkpoint Q4

**Promotes:** S10–S11: Narrative Reader/Markdown/Studio  
**Canonical rows in scope:** N-01, N-02, N-03, N-04, N-05, N-06, N-07, N-08, N-09, MD-01, MD-02, NS-01, NS-02, NS-03, NS-04, NS-05, NS-06, NS-07, NS-08, NS-09, NS-10  
**Visual profile:** `narrative`  
**Next:** S12

## Required read set
- repository `AGENTS.md` and `AI_CONSTITUTION.md`;
- `../00_OWNER_EXECUTION_AUTHORIZATION.md`;
- `../00_MASTER_EXECUTION_SPEC.md`;
- `../04_TEST_AND_VERIFICATION_POLICY.md`;
- this checkpoint;
- `../state/EXECUTION_LEDGER.md`;
- only failing stage/surface packets if evidence requires diagnosis.

Do not reopen all promoted rows merely because the checkpoint is broad.

**Continuous-session economy:** do not reread unchanged global authorities; refresh this checkpoint, ledger, and only the failing stage/surface packet needed by evidence.

## Native behavior batch
No broad native persistence phase solely for visuals.

Use one runner invocation for the selected phases where the existing runner supports batching/build reuse.

## Visual evidence
Run stage-selectable profile `narrative` at required viewport(s).
Compare only reviewed/locked tags.
Do not auto-accept unrelated baselines.

## Extra gates
Focused Narrative tests; `pnpm hardening:narrative-performance`; production build + JS budget. Re-run portable roundtrip only if package UI/contract wiring changed.

## Failure routing
- reproducible product regression → reopen only causally implicated row(s);
- shared primitive regression → smallest primitive fix + representative prior consumers;
- nondiagnostic harness failure → bounded diagnosis, one controlled rerun, deterministic substitute/VERIFICATION_DEBT if repository closure rules allow;
- performance red → reclaim cost; never raise ceiling without owner authority.

## Close
When evidence passes, promote eligible rows from `LOCAL_VERIFIED` to `VERIFIED`, persist command/artifact evidence, close Q4, and continue to **S12** under the same top-level Goal.
