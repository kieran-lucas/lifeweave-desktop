# Phase 1 Exact-Byte Policy

Rust source corrections are taken from the Windows `cargo fmt` artifact that
also produced the compiler diagnostics. The repository update replaces the
four Task 36 Rust files atomically. A subsequent independent Windows run must
pass format, check, clippy, and tests before Phase 1 is accepted.
