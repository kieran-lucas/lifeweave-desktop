# Acceptance

Schema 15 accepts exactly the three IDs, preserves existing Knowledge Dossier rows, and rejects template identity changes. No custom templates, conversion, or Visual Worlds are included.
