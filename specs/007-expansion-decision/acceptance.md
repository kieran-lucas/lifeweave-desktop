# Slice 007 Acceptance

Task 17 passes only when:

- all ten candidates have exactly one recommendation state;
- no more than one candidate is `ACTIVATE_NEXT` or `PROTOTYPE_NEXT`;
- Global Search is the only `ACTIVATE_NEXT` result;
- the hard-filter table is complete;
- weights, priors, uncertainty and seed are disclosed;
- the simulation is described as sensitivity analysis, not proof;
- the prerequisite graph is present in Mermaid and plain text;
- Task 18 remains unimplemented and prohibited pending Product Owner approval;
- no production dependency, migration, IPC, capability, route or UI file changes;
- source integrity and documentation gates pass;
- the final commit is pushed once and the worktree is clean.
