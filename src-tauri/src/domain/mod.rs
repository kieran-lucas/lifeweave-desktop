// Entities, value objects, and domain invariant logic.
// Permitted imports: std, serde (serialization traits only).
// Prohibited: tauri, rusqlite, tokio, filesystem, IPC.

pub mod foundation_record;
pub mod primitives;
