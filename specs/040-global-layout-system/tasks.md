# Task 50 Work Items

- [ ] Preserve schema 27; add no migration, Rust product change, IPC command, capability, or dependency.
- [ ] Produce the UI surface census before any product edit, with exact §41 totals.
- [ ] Audit every current CSS layout authority and record the duplicates and conflicts.
- [ ] Capture measured baseline geometry and screenshots at 1280×720, 1440×900 and 1920×1080.
- [ ] Locate every horizontal-overflow source by measurement rather than inference.
- [ ] Create one layout authority under `frontend/src/app/layout/` and one 4 px-derived ramp.
- [ ] Give every top-level surface exactly one declared page type; leave no unclassified geometry.
- [ ] Remove every page-local maximum width, page gutter, dialog width, and page-level section gap
      from domain CSS.
- [ ] Remove the application root's `width: 100vw`, the measured cause of the 15 px global scrollbar.
- [ ] Reserve the main viewport scrollbar gutter so a capped frame centres deterministically.
- [ ] Rebuild the Task create/edit dialog as a real contained surface with a deterministic field grid.
- [ ] Keep every Task dialog field, its semantics, and its validation unchanged.
- [ ] Separate the Today period headings by layout rather than by literal spaces.
- [ ] Declare the Today timeline row's fourth track explicitly.
- [ ] Bring every dialog onto one modal geometry grammar without changing dialog behaviour.
- [ ] Keep Life graph and Life Edit horizontal scroll local and the inspector reachable.
- [ ] Add the visible Task row Edit control using the existing handler; retain double-click and Enter.
- [ ] Surface or explicitly block every census `MISSING_USER_SURFACE`; expose no internal choreography.
- [ ] Keep Task rows non-card, Today task-first and default, and the sidebar order unchanged.
- [ ] Change no palette, font, icon, radius, shadow, illustration, Visual World, motion, or sound.
- [ ] Add native phase 21 geometry proof at all three viewports in both sidebar states.
- [ ] Prove empty and populated states, and the long-string stress set, both hold.
- [ ] Execute the deliberate geometry break, prove a meaningful failure, restore, prove the pass.
- [ ] Keep frontend tests and axe green; record each evidence class separately and honestly.
- [ ] Hold Task 49's performance ceilings unchanged and measure against them.
- [ ] Review the full diff against the activation SHA with the nineteen closure questions.
- [ ] Keep Task 51 prohibited and untouched.
