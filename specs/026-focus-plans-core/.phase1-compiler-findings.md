# Phase 1 Compiler Findings

- Governance, source, indexes, remote-asset, and security gates pass.
- Frontend baseline passed in the prior run; two unrelated historical tests later timed out and are tracked as flaky regression evidence, not Task 36 changes.
- Rust exact diagnostics identified eight revision storage type errors: `rusqlite` does not implement `ToSql`/`FromSql` for `u64`.
- Correction: keep external DTO revisions as `u64`, use checked `u32` at the SQLite boundary, and convert losslessly.
- Rustfmt exact bytes are applied from the Windows CI artifact.
- `OptionalExtension` is moved into the migration test module to keep non-test builds warning-free.
