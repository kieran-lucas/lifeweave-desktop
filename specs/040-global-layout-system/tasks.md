# Task 50 Work Items

- [x] Preserve schema 27; add no migration, Rust product change, IPC command, capability, or dependency.
- [x] Produce the UI surface census before any product edit, with exact §41 totals.
- [x] Audit every current CSS layout authority and record the duplicates and conflicts.
- [x] Capture measured baseline geometry and screenshots at 1280×720, 1440×900 and 1920×1080.
- [x] Locate every horizontal-overflow source by measurement rather than inference.
- [x] Create one layout authority under `frontend/src/app/layout/` and one 4 px-derived ramp.
- [x] Give every top-level surface exactly one declared page type; leave no unclassified geometry.
- [x] Remove every page-local maximum width, page gutter, dialog width, and page-level section gap
      from domain CSS.
- [x] Remove the application root's `width: 100vw`, the measured cause of the 15 px global scrollbar.
- [x] Reserve the main viewport scrollbar gutter so a capped frame centres deterministically.
- [x] Rebuild the Task create/edit dialog as a real contained surface with a deterministic field grid.
- [x] Keep every Task dialog field, its semantics, and its validation unchanged.
- [x] Separate the Today period headings by layout rather than by literal spaces.
- [x] Stop the Today timeline row growing an undeclared implicit column. Solved with two declared
      tracks — content plus one flex action region — rather than a fourth track, so the row cannot
      grow a new column however many action controls it renders.
- [x] Bring every dialog onto one modal geometry grammar without changing dialog behaviour.
- [x] Keep Life graph and Life Edit horizontal scroll local and the inspector reachable.
- [x] Add the visible Task row Edit control using the existing handler; retain double-click and Enter.
- [x] Surface or explicitly block every census `MISSING_USER_SURFACE`; expose no internal choreography.
- [x] Keep Task rows non-card, Today task-first and default, and the sidebar order unchanged.
- [x] Change no palette, font, icon, radius, shadow, illustration, Visual World, motion, or sound.
- [x] Add native phase 21 geometry proof at all three viewports in both sidebar states.
- [x] Prove empty and populated states, and the long-string stress set, both hold.
- [x] Execute the deliberate geometry break, prove a meaningful failure, restore, prove the pass.
- [x] Keep frontend tests and axe green; record each evidence class separately and honestly.
- [x] Hold Task 49's performance ceilings unchanged and measure against them.
- [x] Review the full diff against the activation SHA with the nineteen closure questions.
- [x] Keep Task 51 prohibited and untouched.
