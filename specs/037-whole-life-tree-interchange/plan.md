# Task 47 Execution Plan

Status: ACTIVE.

## Stage 0 — Activation and baseline

Confirm clean exact baseline and remote parity. Read authority in constitutional order, inspect the
complete Task 42 implementation and regressions, current schema-26 chain, backup/restore authority,
Life Edit, Phase 13, and Task 46 performance contract. Create ADR 0041 and Slice 037; activate
Project State at schema 26; run activation governance; commit and push no product code. Record the
activation performance inventory.

## Stage 1 — Schema and Tree forest authority

Add migration 27 rebuilding only `life_operations` to admit `import_tree`, update startup and
backup authority, and prove exact preservation. Add a Tree-only structural type and validator with
ordered roots, canonical sibling order, derived depth, total reachability, and cross-format
rejection.

## Stage 2 — Bounded export and archive

Reuse private Task 42 primitives where invariants are identical. Build a fixed-query active-forest
snapshot from `life-root`, rebase top-level and internal order after archived exclusions, assemble
documents/assets/tags/links and count-only omissions, then create and self-validate the distinct
checksummed archive under unchanged limits.

## Stage 3 — Atomic append-only import

Validate destination and revision, allocate complete fresh maps, resolve tags and link caps, prove
file feasibility, append every root after existing children, preserve internal order, remap
documents/assets/tags/links, and commit once with one revision increment and one `import_tree`
operation. Prove rollback, exact file ownership, idempotent replay, non-undo, restart, and backup.

## Stage 4 — IPC, bindings, frontend, and regressions

Register exactly five commands and permissions, generate TypeScript normally, add lazy Life Edit
controls and preview dialog, preserve Branch behavior, wire Task 42 cache invalidation and focus,
and cover keyboard, focus, safe failure, retry, size precheck, aggregate copy, and axe.

## Stage 5 — Phase 18 and deliberate break

Add standalone Phase 18 and restart specs using the existing Blob capture. Export a known two-root
forest, create the destination after export, cancel without mutation, confirm the same bytes, prove
fresh identities/order/content/link/source preservation, then prove restart. Break a central root
or remap rule, record meaningful failure, restore, and prove zero residue.

## Stage 6 — Performance, full gates, and product checkpoint

Measure the final bundle, create Task 47 performance baseline/budget evidence in ADR 0034 v2 format,
and preserve every locked ceiling. Run all specified frontend, Rust, installer, native, RC,
governance, source, and diff gates. Inspect the complete activation-to-product diff, commit and push
`implement whole-life tree interchange`.

## Stage 7 — Closure

Write the Task 47 audit with exact command and deliberate-break evidence. Close governance at schema
27 with Task 47/Slice 037 complete, null active spec, `product_owner_gate`, and Task 48 prohibited.
Commit/push closure, record the closure SHA in a final audit-only commit, and verify clean remote
parity.
