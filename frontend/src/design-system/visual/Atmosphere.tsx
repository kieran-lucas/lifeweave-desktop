/**
 * Decorative atmosphere intentionally disabled.
 *
 * The current visual authority uses a flat white canvas and solid blue pigment only. Global SVG
 * ornaments, washes, stars, petals, orbit lines and animated background fields are deliberately
 * absent so product surfaces own the entire composition.
 */
export function Atmosphere() {
  return null;
}
